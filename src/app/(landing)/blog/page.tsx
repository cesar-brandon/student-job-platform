import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog",
  description: "Pagina de Blog",
};

const BlogPage = () => {
  return (
    <div>
      <h1>Blog</h1>
    </div>
  );
};

export default BlogPage;
